{
  "$schema": "https://wails.io/schemas/config.v2.json",
  "name": "{{.ProjectName}}",
  "outputfilename": "{{.BinaryName}}",
  "outputType": "grpcserver",
  "frontend:install": "flutter pub get",
  "frontend:build": "",
  "frontend:dev:watcher": "flutter run",
  "frontend:dir": "frontend",
  "preBuildHooks": {
    "*/*": "sh -c 'cd \"${projectDir}\" && protoc -I proto --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative proto/*.proto && protoc -I proto --dart_out=grpc:\"${frontendDir}\"/lib/src/generated proto/*.proto'",
    "windows/*": "cmd /C \"cd /D ${projectDir} && protoc -I proto --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative proto/*.proto && protoc -I proto --dart_out=grpc:${frontendDir}/lib/src/generated proto/*.proto\""
  },
  "author": {
    "name": "{{.AuthorName}}",
    "email": "{{.AuthorEmail}}"
  }
}
